public class pruebas {
    public int generanumaleatorio(int valormin, int valormax) {
        return (int) ((Math.random()) * (valormax - valormin) + valormin);

    }

    public static void main(String[] args) throws Exception {
        byte edad;
        double velocidadLuz;
        short alumnosTec;
        int Habitantes;
        double Amazon;
        // HARDCODE
        int x = (int) (Math.random() * (1000 - 500) + 500);

    }
}